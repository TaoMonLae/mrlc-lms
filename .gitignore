node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example
# Runtime uploaded e-book files (persisted via Docker volume in production)
data/
# Local Claude Code tool settings — machine-specific, not part of the app
.claude/
